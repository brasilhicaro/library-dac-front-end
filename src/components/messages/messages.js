// import React from 'react';
// import { Messages } from 'primereact/messages';
// const Message = ({ severity, summary, detail }) => {
    // const [messages, setMessages] = useState([]);
  
//     const showMessage = (severity, summary, detail) => {
//       setMessages([{ severity, summary, detail }]);
//     };
  
//     if (messages.length > 0) {
//       return <Messages value={messages} />;
//     }
  
//     return null;
//   };

// export const showSuccess = (title, message) => showMessage('success', title, message);
// export const showInfo = (title, message) => showMessage('info', title, message);
// export const showWarn = (title, message) => showMessage('warn', title, message);
// export const showError = (title, message) => showMessage('error', title, message);


// export default Message;
